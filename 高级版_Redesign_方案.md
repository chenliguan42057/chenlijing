# 薄荷减脂打卡 · 高级版 Redesign 方案（待确认）

> 针对反馈：当前版本过于单薄、缺少层级与高级感。本方案参考现代健康/体重管理 App 的结构，保留浅薄荷绿温柔极简基调，重新设计为「目录导航 + 多页面 + 仪表盘 + 数据可视化」的完整应用形态。
> **Worker 后端（`worker/src/index.js`）无需改动**，只重写前端。

---

## 1. 整体结构：左侧目录 + 顶部栏 + 页面内容区

### 1.1 目录（左侧固定导航 / 移动端抽屉）
| 目录项 | 作用 |
|--------|------|
| 仪表盘 | 总览：关键指标、体重趋势图、今日动态、目标进度 |
| 今日打卡 | 每日记录：体重、睡眠、饮水、心情、备注 |
| 饮食记录 | 每餐食物与热量台账，支持常用食物快捷添加 |
| 运动记录 | 运动项目、时长、估算消耗 |
| 身体数据 | 体重/BMI/腰围历史曲线，目标体重设定 |
| AI 助手 | 减脂问答（保留现有聊天逻辑，整合进新布局） |
| 设置 | 个人资料、数据导入导出、清空缓存、关于 |

### 1.2 页面层级
- **桌面端**：左侧目录始终可见，右侧主内容区切换页面。
- **移动端**：顶部栏左侧汉堡菜单，点击滑出抽屉式目录；主内容区上下滚动。
- 每个页面有独立标题与面包屑感（仅文字标题，不增加复杂度）。

---

## 2. 新增实用功能（挑选自市面健康 App）

| 模块 | 新增功能 | 为什么实用 |
|------|---------|-----------|
| 仪表盘 | 4 个核心指标卡（当前体重、今日摄入、运动消耗、连续打卡） | 一眼掌握今日状态 |
| 仪表盘 | 体重趋势折线图（近 7/14/30 天可切换） | 看见长期变化，减少每日焦虑 |
| 仪表盘 | 目标进度环（目标体重 → 完成百分比） | 有明确目标感 |
| 今日打卡 | 饮水杯数计数器（+ / -） | 减脂期基础但高频的功能 |
| 今日打卡 | 睡眠时长记录 | 睡眠与减脂强相关 |
| 今日打卡 | 心情标签（不错/一般/疲惫） | 情绪与饮食行为相关 |
| 饮食记录 | 每餐食物名称 + 热量输入，自动汇总今日摄入 | 比单纯写文字更量化 |
| 饮食记录 | 常用食物快捷按钮（鸡蛋、米饭、香蕉、牛奶等） | 降低记录门槛 |
| 运动记录 | 运动类型 + 时长 + 估算消耗 | 量化运动贡献 |
| 身体数据 | 自动 BMI 计算（设置页填身高） | 常见健康指标 |
| 身体数据 | 腰围/臀围记录与趋势 | 减脂不止看体重 |
| 设置 | 数据导出/导入 JSON | 换设备或备份不丢数据 |
| 设置 | 一键清空所有本地数据 | 隐私与重置 |

> 说明：所有数据仍只保存在浏览器 `localStorage`，不上传服务器；AI 仍只通过自建 Worker 交互。

---

## 3. 视觉高级感提升（仍保持浅薄荷绿）

- **布局**：从底部 3 标签升级为「左侧目录 + 顶部栏 + 主内容网格」，信息分区清晰。
- **卡片**：白色圆角卡片、0.5px 细边框、柔和投影，组成层级。
- **图表**：自定义 SVG 面积图/折线图 + 圆环进度图，不使用外部图表库，避免依赖与加载失败。
- **字体层级**：大标题 20px / 页面标题 18px / 卡片标题 15px / 正文 14px / 辅助 12px。
- **色彩**：主色 `#5DCAA5` / 深绿 `#0F6E56` / 浅背景 `#F7FDFA` / 卡片白 `#FFFFFF`，统一且克制。
- **动效**：页面切换淡入、卡片 hover 微抬、按钮点击缩放，提升精致感。
- **空状态**：各列表无数据时显示温柔插画/图标与引导文案，不显简陋。

---

## 4. 数据模型调整（localStorage）

```
user            { name, height(cm), targetWeight }
checkins        [{ date, weight, sleep, water, mood, note }]
meals           [{ id, date, mealType, name, calories }]
exercises       [{ id, date, name, minutes, calories }]
bodyLogs        [{ date, weight, waist, hip }]
aiChat          [{ role, content }]     // 保留现有
```

---

## 5. 文件结构（保持相对路径）

```
frontend/
  index.html              应用外壳 + 目录 + 各页面区块
  css/
    style.css             完整设计系统 + 响应式布局
  js/
    config.js             WORKER_URL
    app.js                目录切换、仪表盘、打卡、记录、身体数据、设置
    ai.js                 AI 助手聊天逻辑（适配新布局）
    charts.js             SVG 趋势图/进度环绘制（纯原生）
  assets/
    icon.svg
```

---

## 6. 与硬性规则的兼容性

- **密钥安全**：只改前端，Worker 逻辑不变，前端仍无密钥。
- **AI 角色**：System Prompt 仍在 Worker，人设不变。
- **Worker 代理**：AI 请求仍只发往自建 Worker。
- **相对路径**：所有资源仍使用相对路径，GitHub Pages 兼容。
- **异常处理**：保留现有额度/网络/模型错误提示。

---

## 7. 待确认问题

1. **是否确认以上目录 + 功能清单？**（确认后我按此重写全部前端）
2. **是否需要「热量估算」自动计算？**（例如输入食物重量后自动估算热量；这会依赖固定食物热量表，简单但有限）
3. **是否需要「深色模式」切换？**（高级感选项，但会增加 CSS 量）
4. **图表数据点**：体重趋势默认显示近 14 天，是否保留 7/30 天切换？

---

## 8. 下一步

你确认（或调整）后，我立即重写 `frontend/` 全部文件，完成后重新本地校验并交付最终源码。
