# 薄荷减脂打卡 · v3 卡通多彩版功能方案（待确认）

> 基于你的新想法整理：照片上传、卡通五颜六色风格、小鸡仔成长状态、未打卡抱怨梗、个性化推荐（饮食/运动/作息）、图片教程/视频链接、好友同步。
> 本方案先列清楚**哪些能直接做、哪些有技术限制**，避免盲目开工。

---

## 1. 视觉方向：卡通多彩版

### 整体风格
- 从「浅薄荷绿极简」升级为「卡通游乐场」风格：大圆角、柔和糖果色、手绘感图标、气泡卡片。
- 每个页面/模块使用不同主色：

| 页面 | 主色 | 情绪 |
|------|------|------|
| 仪表盘 | 暖黄色 #FFD66B | 元气、总览 |
| 今日打卡 | 薄荷绿 #7FDEB5 | 健康、完成 |
| 饮食记录 | 蜜桃粉 #FF9FA6 | 美食、温柔 |
| 运动记录 | 天空蓝 #7EC8E3 | 清爽、活力 |
| 身体数据 | 薰衣草紫 #C5A8FF | 数据、成长 |
| AI 助手 | 奶油青 #88D8B0 | 陪伴、治愈 |
| 设置 | 浅灰蓝 #BCCCDC | 安静、工具 |

- 全局字体更圆润，按钮像糖果块，卡片带轻微弹跳动效。

---

## 2. 新增功能清单与技术可行性

### ✅ A. 照片上传（纯前端本地存储）
- **适用位置**：今日打卡（体重/体型自拍）、饮食记录（餐食照片）、运动记录（运动打卡照）。
- **实现方式**：`<input type="file" accept="image/*">` 读取后**压缩成缩略图 base64**，存入 `localStorage` 或 `IndexedDB`。
- **限制说明**：
  - GitHub Pages 是静态站点，没有后端存储空间；照片只能存在用户自己手机里。
  - `localStorage` 总容量通常 5MB 左右，建议单张压缩到 100KB 以内，最多存 20–30 张；超限后提示用户清理。
  - 换手机/清浏览器会丢失（可通过导出 JSON 备份，但照片会让备份文件很大）。
- **建议**：先做「今日打卡」和「饮食记录」的照片，运动记录可后加。

### ✅ B. 小鸡仔成长状态（本地状态机）
- **核心逻辑**：
  - 小鸡仔有「成长值」，每天完成打卡 +10 点。
  - 连续打卡有加成：连续 3 天额外 +5，连续 7 天额外 +15。
  - 漏打卡一天：成长值 -5，小鸡仔状态变成「蔫了」。
  - 成长值满 100 升级：鸡蛋 → 小鸡 → 青年鸡 → 华丽大鸡（可扩展）。
- **互动文案**：
  - 打卡后：随机弹出搞笑梗，例如「今天自律了，鸡你太美！」「本鸡正式宣布：你是减脂界的卷王。」
  - 漏打卡：随机抱怨梗，例如「一天不见，鸡都瘦了…」「你是不是背着我去吃炸鸡了？」
- **展示位置**：仪表盘常驻卡片 + 今日打卡页面顶部。

### ✅ C. 未打卡提醒 / 违约机制
- 打开页面时检测今天是否打卡。
- 未打卡：小鸡仔蔫掉动画 + 文案吐槽 + 红色「未打卡」标记。
- 已打卡：小鸡仔精神 + 金色「今日已打卡」徽章。

### ✅ D. 个性化推荐（饮食 / 运动 / 作息）
- **数据来源**：用户的身高、体重、目标体重、BMI、今日摄入/运动。
- **推荐内容**：
  - 饮食：根据热量缺口推荐一日三餐搭配（调用本地食物库）。
  - 运动：根据 BMI 和目标推荐低/中/高强度运动。
  - 作息：根据睡眠记录推荐入睡时间、饮水提醒。
- **教程形式**：
  - 图片教程：用 SVG/插画展示几个动作步骤（如深蹲、开合跳）。
  - 视频链接：弹出外链到 Bilibili/YouTube 的精选跟练视频（链接可配置，不需要后端）。
- **实现方式**：本地规则引擎 + 少量预置推荐模板，不需要 AI（但也可以让 AI 按钮再生成一份）。

### ⚠️ E. 好友同步（技术限制最大）
- **问题**：GitHub Pages 是纯静态托管，没有数据库；Cloudflare Worker 目前只用于 AI 问答，也没有持久化数据库。
- **可行替代方案**：
  1. **假数据演示**：做一个「好友广场」页面，展示几个示例好友状态（仅用于预览效果，不能真实同步）。
  2. **导出分享卡**：把自己的打卡状态生成一张图片/JSON，发给朋友手动查看。
  3. **接入 Cloudflare D1 数据库**：需要额外付费/学习成本，且 Worker 代码要重构，增加用户鉴权，复杂度较高。
  4. **接入第三方协作平台**：例如用飞书/Notion/腾讯文档作为后端，但均需账号和 API 权限。
- **建议**：本次先做方案 1（演示好友广场）或方案 2（分享卡），真实同步放到后续二期。

---

## 3. 确认事项（需要你拍板）

| # | 问题 | 我的建议 |
|---|------|---------|
| 1 | 是否确认改成「卡通多彩」风格？ | 确认后我会重写整套 CSS 和图标。 |
| 2 | 照片功能做哪几个页面？ | 建议先做「今日打卡 + 饮食记录」。 |
| 3 | 照片存储限制是否接受？ | 照片存在本地、有容量上限、换设备丢失。 |
| 4 | 小鸡仔成长系统是否确认？ | 包含打卡成长、漏打卡蔫掉、搞笑文案。 |
| 5 | 推荐功能范围？ | 建议先做「今日饮食推荐 + 运动推荐 + 作息小贴士」，图片教程用 SVG，视频用外链。 |
| 6 | 好友同步怎么做？ | 建议本期先做「示例好友广场」或「状态分享卡」，真实同步需要后端，放到二期。 |

---

## 4. 实施步骤（确认后执行）

1. **设计系统升级**：重写 `style.css`，卡通糖果色、每个页面主题色、圆角动效。
2. **重构页面**：在现有 7 个页面基础上调整视觉，增加小鸡仔卡片、照片上传区。
3. **新增状态系统**：小鸡仔成长值、打卡检测、搞笑文案库。
4. **推荐引擎**：本地规则生成饮食/运动/作息推荐，SVG 动作教程，外链视频。
5. **好友功能**：实现演示版好友广场或状态分享卡。
6. **联调与校验**：`node --check` + 浏览器预览。

---

## 5. 预期交付物

- 更新后的 `frontend/` 全部源码（视觉 + 功能）
- `worker/src/index.js` 可保持不变（除非要做推荐时让 AI 也参与）
- 更新 `部署文档_方案3.md` 中的新功能说明
- 本方案文档作为设计依据

> 下一步：你确认上方 6 个确认项（可以直接回复数字选项），我就开始按 v3 重写。
