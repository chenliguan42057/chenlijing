# 减脂打卡网站 · 方案3「全天候网页内置 AI 减脂助手」技术方案（试点确认稿）

> 状态：**试点方案，待你审核确认**。确认满意后，我再完整生成 ② Workers 部署代码 + ③ 网站全部前端源码 + ④ 图文部署文档。

---

## 1. 执行摘要（一句话）

用 **GitHub Pages（静态前端）＋ Cloudflare Workers（免费 Serverless 代理＋AI 推理）** 的组合，给现有减脂打卡网站新增一个独立的「AI 减脂助手」标签页：前端只跟自建 Worker 通信，Worker 内部调用 Cloudflare 免费模型 `@cf/meta/llama-3.1-8b-instruct`，**密钥与凭证永不下发到浏览器**。

---

## 2. 整体架构

```
┌─────────────────────┐         HTTPS（仅访问自建 Worker）        ┌──────────────────────────┐
│   浏览器 / 手机端     │  ───────────────────────────────────▶   │   Cloudflare Workers      │
│  GitHub Pages 静态页 │    POST /chat  {messages:[...]}           │  （你的专属代理 + AI）      │
│  - 打卡 / 记录        │                                          │   ├─ CORS 校验来源          │
│  - AI 助手标签页      │  ◀───────────────────────────────────   │   ├─ 调用 Workers AI        │
│  - 本地缓存聊天记录   │     {reply:"..."}  /  {error:"quota"}     │   │   @cf/meta/llama-3.1-8b  │
└─────────────────────┘                                          │   └─ 异常处理 + 友好提示    │
                                                                    └──────────────────────────┘
                                               ✅ 模型绑定在 Worker 内，前端无任何 Key / Token
```

**关键点**：浏览器永远只请求你自己的 `xxx.workers.dev` 地址；第三方大模型 API 由 Worker 代为调用，前端代码里不会出现任何密钥。

---

## 3. 硬性规则逐条落实（对照你的 7 条要求）

| # | 你的要求 | 本方案落地方式 |
|---|---------|---------------|
| 1 | 密钥/凭证不进前端 | Workers AI 通过 **Wrangler 绑定 `[ai] binding="AI"`** 调用，无需任何 API Key；前端只持有一个**公开的 Worker 地址**（非保密），写在 `js/config.js` 一处，方便你改。 |
| 2 | AI 角色：专业减脂助手，温柔简洁，适配女生 | Worker 内写死一段 **System Prompt**（中文、温柔、简洁、聚焦饮食热量/运动方案/减脂常识，不诊断疾病、不推荐极端节食）。 |
| 3 | 浅薄荷绿温柔极简，手机优先，风格统一 | 全站用一套 CSS 变量主题（薄荷绿主色 + 留白 + 圆角卡片），移动端优先布局；AI 标签页沿用同一套组件样式。 |
| 4 | 独立页面标签 + 聊天记录本地缓存 | 单页应用底部 Tab 导航含「打卡 / 记录 / AI 助手」；AI 对话历史用 `localStorage` 缓存，可一键清空。 |
| 5 | 全部走自建 Worker 代理 | 前端 `fetch(WORKER_URL + '/chat')`，**不直接对接任何第三方模型 API**。 |
| 6 | 异常处理：额度耗尽 / 网络失败友好提示 | Worker 捕获 Workers AI 报错区分「额度耗尽(429)」与「模型错误」；前端 `try/catch` 捕获网络失败，均给出温柔文案。 |
| 7 | 输出顺序 ①②③④ | 确认后依次交付：方案文档 → Worker 代码 → 前端源码 → 图文部署文档。 |

---

## 4. 目录结构与文件清单

### 4.1 前端（放进你的 GitHub Pages 仓库根目录，全部相对路径）
```
/index.html            主站单页：打卡 + 记录 + AI助手 三个 Tab
/css/style.css         浅薄荷绿主题，手机优先，统一组件样式
/js/config.js          ★ 唯一需你改的地方：WORKER_URL（你的 Worker 地址）
/js/app.js             打卡 / 记录 逻辑 + localStorage 数据
/js/ai.js              AI 助手聊天逻辑 + 对话本地缓存
/assets/icon.svg       站点图标（可选，薄荷绿风格）
README.md              仓库说明 + 一键部署指引（对应交付物④精简版）
```

### 4.2 Cloudflare Worker（单独一个小项目，不在 GitHub Pages 仓库内）
```
worker/
  wrangler.toml        名称 / compatibility_date / [ai] 绑定 / 可选 ALLOWED_ORIGIN
  src/index.js         fetch 处理器：CORS → 解析 messages → 调 AI → 返回 / 异常
  package.json         （可选，便于本地预览）
```

---

## 5. 关键技术点

### 5.1 Cloudflare Workers（免费额度，已核实）
- **免费档**：Workers Free 每日赠送 **10,000 Neurons**，无需绑卡；每日 00:00 UTC 重置。
- 模型 `@cf/meta/llama-3.1-8b-instruct` 约 **输入 25,608 / 输出 75,147 Neurons 每百万 token**。
- 粗算：一次普通对话（≈600 输入 + 250 输出 token）约 **34 Neurons** → 免费档约可支撑 **200–300 轮/天** 的个人使用，足够打卡小站。
- 超额后 Workers AI 返回错误 → 我们转成「今日额度已用完」友好提示，不影响网站其他功能。

### 5.2 Worker 代码思路（伪代码）
```js
// src/index.js（示意，确认后给完整版）
export default {
  async fetch(request, env) {
    // 1. CORS：仅放行你的 GitHub Pages 来源（env.ALLOWED_ORIGIN，默认 *)
    // 2. 处理 OPTIONS 预检
    // 3. 解析前端发来的 { messages:[{role,content}] }
    // 4. 注入 System Prompt（减脂助手人设）
    // 5. env.AI.run("@cf/meta/llama-3.1-8b-instruct", { messages })
    // 6. 成功 → 返回 { reply }
    //    额度耗尽/模型错误 → 返回 { error:"quota" | "model" } + 对应 HTTP 状态
  }
}
```
- 关键点：**`env.AI` 是平台绑定，不是密钥**，前端拿不到；这就是规则①的根本保障。
- v1 采用**非流式**返回（稳定、简单、边缘节点够快）；如需「逐字输出」体验可作为后续增强。

### 5.3 前端 AI 标签页思路
- 聊天UI：消息气泡（用户右/助手左）、输入框、发送按钮、加载动画。
- 每次发送把「历史 + 新消息」一起发给 Worker，Worker 负责拼 System Prompt 与生产回复（前端不存系统提示，避免被改）。
- 本地缓存：`localStorage` 保存对话数组，进入页面自动恢复；提供「清空对话」按钮。
- 异常处理文案：
  - 网络失败：`网络好像开小差了，检查一下网络再试试～`
  - 额度耗尽：`今天的 AI 小助手额度用完啦，明天 0 点会自动恢复，先去打卡记录一下吧 🌿`
  - 其他错误：`小助手暂时有点忙，稍后再来聊～`

### 5.4 安全与防滥用（轻量）
- CORS 限定为你自己的 Pages 域名，避免被他人白嫖接口。
- 聊天记录本地保存，重复问题不重复消耗额度。
- 不引入任何第三方 JS 库（纯原生 JS），减小攻击面、也更符合「极简」。

---

## 6. 分阶段实施步骤（确认后执行）

| 阶段 | 内容 | 产出 |
|------|------|------|
| ① 方案确认 | 你审核本稿 | 本文件 |
| ② Worker 部署代码 | 写 `wrangler.toml` + `src/index.js`，含 System Prompt 与异常处理 | 交付物② |
| ③ 前端源码 | 写 `index.html` + `css/style.css` + `js/*`，接入 AI 标签与本地缓存 | 交付物③ |
| ④ 本地联调 | 用 Worker 预览地址自测对话/异常分支 | 验证通过 |
| ⑤ 部署文档 | 图文教你在 Cloudflare 后台粘贴 Worker + 推代码到 GitHub Pages | 交付物④ |
| ⑥ 上线 | 你按文档操作，我全程可答疑 | 网站可用 |

---

## 7. 待你确认 / 澄清的事项（重要）

1. **现有网站代码**：当前工作区为空，我未看到你原有的减脂打卡网站源码。
   - 若**没有现成代码** → 我按薄荷绿极简风格**从零全新构建**完整站点（打卡＋记录＋AI 助手）。
   - 若**已有代码** → 请给我仓库地址或把代码放进工作区，我改为**增量改造**，保留你原有设计。
2. **Worker 部署方式**（影响文档写法）：
   - 推荐 **Cloudflare 后台粘贴代码部署**（零安装、无需 Node/命令行，最贴合「不额外自学技术」）。
   - 备选：本地用 Wrangler CLI 上传（需装 Node）。
3. 是否需要**逐字流式输出**（更像真人打字）？v1 默认非流式，稳定优先；需要的话我加。

---

## 8. 最终交付物清单（对应你的要求 ①②③④）

- ① **本技术方案文档**（即本文件，试点确认用）
- ② **Cloudflare Workers 完整部署代码**（`wrangler.toml` + `src/index.js`，含 System Prompt 与全部异常分支）
- ③ **改造后减脂打卡网站全部前端源码**（相对路径，含 AI 助手标签与本地缓存）
- ④ **分步部署操作文档**（图文、零基础可操作：注册 Cloudflare → 粘贴 Worker → 推 GitHub Pages → 填 Worker 地址 → 上线）

> 下一步：你确认本方案（并回复上面 3 个澄清项），我就开始生成 ② 与 ③ 的完整源码，并在本地联调无误后给你 ④。
