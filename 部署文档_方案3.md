# 减脂打卡网站 · AI 减脂助手 部署文档（零基础·图文步骤）

> 适用方案：前端 GitHub Pages ＋ Cloudflare Workers 免费代理（模型 `@cf/meta/llama-3.1-8b-instruct`）。
> 本教程采用「Cloudflare 后台粘贴代码」方式，**不需要安装任何软件、不需要敲命令行**。

---

## 总览：你要做 3 件事
1. 🟢 在 Cloudflare 后台创建一个 Worker，把代码粘进去（得到一串网址）。
2. 🟢 把那串网址填进网站的 `js/config.js`。
3. 🟢 把网站文件推到 GitHub，开启 Pages（得到你的网站网址）。

---

# 第一部分：部署 AI 代理（Cloudflare Worker）

### 步骤 1 · 注册 Cloudflare 账号（免费）
1. 打开 https://dash.cloudflare.com/sign-up
2. 用邮箱注册，**免费版就够用**，无需绑卡。
3. 注册后进入控制台首页。

### 步骤 2 · 新建一个 Worker
1. 左侧菜单点 **「Workers 和 Pages」**（或 Workers & Pages）。
2. 点右上角 **「创建」/ Create**。
3. 选择 **「Worker」** → 点 **「创建 Worker」**。
4. 给 Worker 起个名字，例如 `fatloss-ai`（名字会决定你的网址前缀）。
5. 暂时点 **「部署」**（先随便部署一个空模板，后面再改代码）。

### 步骤 3 · 粘贴代码
1. 部署后会进入这个 Worker 的详情页，点 **「编辑代码」**（或 Quick Edit）。
2. 把左侧编辑器里的默认代码**全部删除**。
3. 打开本仓库 `worker/src/index.js` 文件，复制**全部内容**，**粘贴**进去。
4. 点右上角 **「部署」/ Deploy** 保存。

### 步骤 4 · 添加 AI 绑定（关键，不加则 AI 用不了）
1. 回到该 Worker 详情页，点顶部 **「设置」/ Settings**。
2. 找到 **「变量和绑定」/ Variables and Bindings** 区域，点 **「添加」/ Add**。
3. 变量类型选 **「AI」**（Workers AI），变量名填 **`AI`**（必须大写 AI）。
4. 保存。

> 这一步就是“密钥”所在：模型通过平台绑定调用，**代码里没有任何 API Key**，前端也拿不到。

### 步骤 5 ·（可选）限制只允许你的网站访问
1. 仍在「设置 → 变量和绑定」，添加一个 **「环境变量」/ Environment Variable**：
   - 名称：`ALLOWED_ORIGIN`
   - 值：你的 GitHub Pages 地址，例如 `https://你的用户名.github.io`
2. 保存并重新部署一次。
3. 不填也行（默认允许所有来源），但填了更安全，防止别人白嫖你的接口。

### 步骤 6 · 复制你的 Worker 网址
1. 在 Worker 详情页顶部，找到类似这样的地址并**复制**备用：
   ```
   https://fatloss-ai.你的用户名.workers.dev
   ```
2. 也可以直接在浏览器打开它，看到 `OK - 薄荷减脂 AI 代理运行中` 就说明部署成功 ✅

---

# 第二部分：配置网站前端

1. 用记事本 / VS Code 打开本仓库 `frontend/js/config.js`。
2. 把这一行改成你的 Worker 地址：
   ```js
   WORKER_URL: "https://fatloss-ai.你的用户名.workers.dev",
   ```
   > 这是**公开网址不是密钥**，放在前端没问题；所有 AI 请求只发往这个地址。
3. 保存。

---

# 第三部分：发布到 GitHub Pages

### 步骤 1 · 新建仓库
1. 登录 https://github.com ，点右上角 **「+」→ New repository**。
2. 仓库名随便起，例如 `fatloss-checkin`。
3. 选 **Public**（Pages 免费需要公开仓库），点 **Create repository**。

### 步骤 2 · 上传网站文件
1. 进入新仓库，点 **「Add file → Upload files」**。
2. 把本仓库 `frontend/` 目录里的**全部内容**（`index.html`、`css/`、`js/`、`assets/`）拖进去上传。
   - ⚠️ 注意：要把 `css`、`js`、`assets` 这些**文件夹一起上传**，保持相对路径结构。
3. 底部写个提交说明，点 **「Commit changes」**。

### 步骤 3 · 开启 Pages
1. 仓库顶部点 **「Settings」→ 左侧 **「Pages」**。
2. **Branch** 选 `main`（或 `master`），**文件夹** 选 `/ (root)`。
3. 点 **「Save」**。
4. 等待约 1–2 分钟，页面会显示你的网站地址：
   ```
   https://你的用户名.github.io/fatloss-checkin/
   ```
5. 打开它，底部切到 **「AI助手」** 标签就能聊天啦 🌿

---

# 第四部分：验证 & 常见问题

### ✅ 怎么确认成功了？
- 打开你的网站 → 点「AI助手」→ 输入「一根香蕉多少卡路里？」→ 能收到温柔回复即成功。
- 或直接浏览器打开 Worker 地址，看到 `OK - 薄荷减脂 AI 代理运行中`。

### ❓ 聊天一直转圈 / 报“网络开小差”
- 检查 `js/config.js` 里的 `WORKER_URL` 是否填对、有没有多余的空格。
- 确认 Worker 已「部署」成功（刷新 Worker 详情页看状态）。
- 手机和电脑都要联网。

### ❓ 提示“额度用完”
- Cloudflare 免费档每天赠送 10000 Neurons（约 200–300 轮对话），**北京时间约早上 8 点（UTC 0 点）重置**。
- 个人打卡小站一般够用；如不够，可在 Cloudflare 升级 Workers Paid（按量计费，很便宜）。

### ❓ 网页样式乱了 / 图片不显示
- 确认 `css`、`js`、`assets` 三个文件夹和 `index.html` 在**同一层目录**且已上传。
- 所有引用都是相对路径，不要改成绝对路径。

### ❓ 想改 AI 助手的话术/人设
- 改 `worker/src/index.js` 顶部的 `SYSTEM_PROMPT`（温柔程度、专长范围都在这里），改完重新「部署」即可。

---

## v3 卡通多彩版 · 新增功能说明
> 部署方式（Worker + Pages）与上面完全一致，**无需额外操作**。下面是本版新增的能力：

1. 🎨 **卡通多彩主题**：每个页面独立主题色（仪表盘暖黄 / 今日打卡薄荷绿 / 饮食蜜桃粉 / 运动天空蓝 / 身体薰衣草紫 / AI奶油青 / 设置浅灰蓝），圆润气泡卡片 + 弹跳动效。
2. 🐤 **小鸡仔成长系统**：每天打卡 +10 成长值，连续 3/7 天额外 +5/+15；漏打卡 -5 并「蔫掉」抱怨。**仅本机逻辑，不依赖后端**。
3. 📷 **照片上传**：今日打卡（体型自拍）、饮食记录、运动记录均可拍照，图片压缩为缩略图 base64 存本地（`localStorage`）。建议在设置页关注「存储用量」，照片较多时及时清理。
4. 💡 **个性化推荐（今日推荐页）**：根据身高/体重/BMI/目标与今日摄入，生成饮食搭配、运动方案（含 SVG 动作教程 + B 站跟练视频链接）、作息建议。纯本地规则引擎，无需 AI。
5. 👯 **好友广场（演示）**：展示 4 个示例好友状态，点击查看详情。真实好友同步需后端数据库，列入后续版本。
6. 📸 **状态分享卡**：仪表盘点「生成分享卡」可把今日状态绘制成图片并保存到本机。

> 注意：`worker/src/index.js` 在本版**无需改动**，AI 仍走原有代理。

---

## 文件清单（交付物对应关系）
- ② Cloudflare Workers 部署代码：`worker/wrangler.toml`、`worker/src/index.js`、`worker/package.json`
- ③ 网站前端全部源码：`frontend/` 下所有文件
  - `index.html`（应用外壳 + 8 个页面：仪表盘/今日打卡/饮食/运动/身体数据/今日推荐/AI助手/个人资料）
  - `css/style.css`（卡通糖果色设计系统）
  - `js/config.js`（Worker 地址，唯一需手动修改）
  - `js/app.js`（全部业务逻辑 + 小鸡仔/照片/推荐/分享卡）
  - `js/charts.js`（原生 SVG 图表）
  - `js/ai.js`（AI 助手聊天）
  - `assets/icon.svg`
  - `README.md`
- ④ 本部署文档：`部署文档_方案3.md`
- ① 技术方案：`方案3_AI减脂助手_技术方案.md`
- Redesign 方案：`高级版_Redesign_方案.md`
- v3 功能方案：`v3_卡通多彩版_功能方案.md`

---

## v3.1 更新（本次迭代）

### 变更要点
1. **删除「好友广场」**：纯静态 GitHub Pages 无法实现「无前端密钥的多账号实时切换」。若把 GitHub Personal Access Token 写进前端，会破坏「前端零密钥」安全红线（密钥只应存在于 Cloudflare Worker 环境变量）。经评估不可行，故删除该页面。
2. **「设置」改名为「个人资料」**：侧边栏导航与页面标题同步更新（图标改为 👤）。
3. **「今日推荐」新增身体情况调查**：进入推荐页先填 6 题小调查（性别 / 年龄段 / 日常活动量 / 饮食偏好 / 减脂目标 / 身体情况），结果存于 `localStorage`（`mint_survey`）。饮食 / 运动 / 作息推荐均依据调查结果生成；可随时「重新调查」。
4. **饮食记录：具象分量 + 多样食物 + 可滚动可编辑历史**
   - 重量(g) 改为点选具象单位（一个拳头 / 一碗 / 一杯 / 一勺 / 一块 / 半份 / 一根），自动换算克数并估算热量；仍可用「自定义克数」。
   - 食物库扩充至约 60 种，含煎 / 炒 / 炸 / 油 / 蒸 / 煮 / 凉拌等做法（如炒青菜、煎牛排、麻婆豆腐、清蒸鱼、油炸虾…）。
   - 历史列表可滚动（不再一展到底），支持按日期切换查看任意一天，且每条记录可「✎ 一键编辑保存」或删除，所有历史记录均保留。
5. **运动记录**：与饮食同构——日期切换、可滚动历史、一键编辑保存。

### 验证
- `node --check` 三个 JS 文件语法通过。
- jsdom 冒烟测试：8 个页面逐一渲染无运行时错误；饮食新增自动算得 174 kcal、编辑保存成功；运动新增+编辑成功；调查提交后推荐（饮食 3 项 / 运动 2 项 / 作息 3 项）正常生成；「个人资料」改名生效；「好友广场」页面已移除。

---

## v3.1 补充微调（C 项 · 推荐文案口吻 / 食物库 / 历史筛选）

### 变更要点
1. **C-1 扩充食物库**：`FOOD_DB` 从约 60 种扩到约 170 种。新增大量家常菜（清炒菠菜、蚝油生菜、手撕包菜、干锅菜花、红烧豆腐、凉拌木耳…）、外食（汉堡、披萨、寿司、饭团、饺子、包子、葱油饼…）、零食加餐（薯片、冰激凌、坚果、瓜子、啤酒、红酒、拿铁…）与高热量陷阱（沙拉酱、花生酱、食用油、蜂蜜…）。`datalist` 自动同步，新增食物均可自动估算热量（例：披萨 150g = 399 kcal）。
2. **C-2 推荐文案口吻**：饮食 / 运动 / 作息推荐的提示语改为更温柔、更有陪伴感、像朋友碎碎念鼓励的口吻（保留具体可执行信息），贴合卡通品牌调性；推荐页引导语与「已定制」提示同步调暖。
3. **C-3 历史记录筛选**：饮食列表新增「全部 / 早餐 / 午餐 / 晚餐 / 加餐」餐别筛选；运动列表新增「全部 / 有氧 / 力量 / 柔韧 / HIIT」类型筛选。筛选与日期切换叠加生效；当日总量统计仍按全天计算，空筛选态给出「该餐别/类型还没有记录」提示。

### 验证
- `node --check` 语法通过。
- jsdom 冒烟测试（扩展版 `smoke_v31.js`）：0 运行时错误。
  - 饮食筛选：all=1 / 午餐=0 / 早餐=1 ✓
  - 运动筛选：all=1 / 力量=0 / 有氧=1 ✓
  - 食物库新项 披萨(150g)=399 kcal ✓；food-list datalist 选项数 = 170（扩充代理指标）✓
  - 推荐 3 类条目正常生成、调查提交状态「已定制」✓

---

## v3.1 文案活泼化 + 本地部署准备（本次「B 更活泼 / C 部署」）

### B · 推荐文案改为更活泼
- `buildDietRec` / `buildExerciseRec` / `buildScheduleRec` 的 tip 与强度语、推荐页引导语、调查「已定制」提示，全部改为更俏皮、带 emoji、有动作感的口吻（如「把代谢小火炉点着啦🔥」「脂肪燃到冒烟🔥」「咕咚咕咚补起来💧」）。
- 验证：`node --check` 通过；jsdom 冒烟测试 0 错误，推荐 3 类条目正常生成。

### C · 部署（✅ 全部完成，站点与 AI 助手均已上线）
- 仓库已就绪并**已推送成功**：`https://github.com/chenliguan42057/chenlijing.git`，分支 `main`，GitHub API 已确认 `index.html`(29.8KB) 及 `css/ js/ assets/` 位于仓库根目录（GitHub Pages root 源可直接识别）。
- 推送方式：使用你提供的 PAT 一次性鉴权推送，**令牌未写入 `.git/config`、未落盘**；远端原本仅有自动生成的 `README.md`，已用**合并（非强推）**方式并存，未覆盖你任何内容。
- **A · GitHub Pages（✅ 已完成）**：经 GitHub API 确认 Pages 已开启且 `status: built`，源分支 `main`、目录 `/`，站点地址 **`https://chenliguan42057.github.io/chenlijing/`** 已可直接访问（打卡/饮食/运动/推荐/个人资料均可用）。
- **B · Worker / AI 助手（✅ 已完成）**：你已在 Cloudflare 部署 Worker 并把网址 `https://chenliguan.3341644038.workers.dev` 发我；我已将其填入 `js/config.js` 的 `WORKER_URL` 并推送（`b0792dc..021d871 main -> main`），GitHub API 核验线上文件已更新。前端 `ai.js` 访问 `WORKER_URL + /chat`，与 Worker 路由/返回格式（CORS 对 `*` 开放）完全匹配。
  - ⚠️ **前提**：你的 Worker 须已按说明在「Settings → Bindings」添加名为 **`AI`** 的 Workers AI 绑定；若未加，AI 聊天会报错（届时 Worker 详情页会有红色提示，补上绑定再 Deploy 即可，前端无需改动）。
- ⚠️ **安全**：本次使用的 PAT 已明文出现在对话中，建议部署全部完成后去 GitHub → Settings → Developer settings → PAT 处 **Revoke** 作废，换一个新令牌或本地配 SSH 后再推，更安全。
- 注：若想改回「温柔陪伴」或「专业克制」口吻，仅需回改 `buildDietRec/buildExerciseRec/buildScheduleRec` 与 `index.html` 调查引导语文本，不影响逻辑。

### D · 双站点自动同步自动上传（✅ 已完成）
- 背景：复制出副本仓库 `chenliguan42057/chenlixia`（内容与 chenlijing 完全一致，仅历史多了合并提交），并在其上**已开启 GitHub Pages**。
- 两个站点（均为 `Deploy from a branch: main / /root` 模式）：
  - 主站：`https://chenliguan42057.github.io/chenlijing/`（HTTP 200）
  - 镜像站：`https://chenliguan42057.github.io/chenlixia/`（HTTP 200）
- **自动部署**：GitHub Pages 分支部署模式内置「push 即自动重新构建」，无需任何手动操作。
- **自动同步**：在 `chenlijing` 添加 GitHub Actions 工作流 `.github/workflows/sync-to-chenlixia.yml`——**每次 push 到 main**，自动把最新代码推送到 `chenlixia`，其 Pages 随之自动构建。即：**一处提交，两个网站自动同步更新**。
- 实现要点（踩坑记录，供维护）：
  1. 令牌存于 chenlijing 的 Actions secret `SYNC_TOKEN`（libsodium sealed-box 加密，经 API 创建，不进入仓库/前端）。
  2. Actions runner 会拦截「URL 内嵌 ghp_ 令牌」（回退为 bot 凭据 403），且 actions/checkout 会注入 bot 的 `http.https://github.com/.extraheader`（与自建 header 冲突报 `Duplicate header` 400）。最终方案：`git config --local --unset-all http.https://github.com/.extraheader` 后，用 `-c credential.helper= -c http.https://github.com/.extraheader="AUTHORIZATION: basic $(printf 'x-access-token:%s' "$TOKEN" | base64 -w0)"` 推送。
  3. 同步使用 `--force-with-lease`（先 fetch 目标）——镜像语义但拒绝覆盖「未感知的新提交」。
  4. **已禁用 chenlixia 的 Actions 执行**（`actions/permissions enabled=false`），防止镜像仓库上的同一工作流自我循环触发。
- 日常使用：更新代码只需提交并 push 到 `chenlijing` 的 `main`，两个网站自动同步更新；`chenlixia` 也可独立提交（但下次同步会以 chenlijing 为准覆盖）。


